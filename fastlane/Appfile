require "dotenv"
Dotenv.load(File.expand_path("../../.env", __dir__))

app_identifier(ENV.fetch("APP_IDENTIFIER"))
apple_id(ENV.fetch("APPLE_ID"))

itc_team_id(ENV.fetch("ITC_TEAM_ID"))
team_id(ENV.fetch("TEAM_ID"))
